<!-- translated-from: CONTRIBUTING.md@6d448e0354eca2ee207141ba62c13cafeb31ae62 -->

# 기여하기

관심 가져주셔서 감사합니다! 이 프로젝트는 수동적 ActionCable 관찰로 프로그래머스 풀이 이력을
기록합니다. 기여 전에 두 가지만 알아두세요:

1. **여기서 YAGNI는 헌법입니다.** 메인테이너의 시간이 병목이라, 대부분의 기능 제안은 가치가
   없어서가 아니라 비용 때문에 거절됩니다. 이슈부터 여세요 — 논의 없는 기능 PR은 대개
   닫힙니다.
2. **프로토콜은 비공개이고 불안정합니다.** 프로토콜 관련 주장에는 실측 증거가 필요합니다 —
   "될 겁니다"는 받지 않고, "X번 문제에서 Y일에 확인했습니다"를 받습니다.

## 개발 환경

- **JDK 25** 와 `gh` CLI. 나머지는 Gradle 래퍼가 다 해줍니다. `java -version` 이 25라고
  말해야 하고, 21이면 코드 한 줄 쓰기 전에 빌드가 먼저 실패합니다
- 클론 후 한 번: `git config core.hooksPath .githooks`
  (Claude Code 사용자는 SessionStart 훅이 자동으로 해줍니다)

푸시 전에 네 개가 모두 0으로 종료해야 합니다 — CI도 같은 파일을 돌립니다:

```bash
./scripts/check.sh && ./scripts/test.sh && ./scripts/build.sh && ./scripts/guards.sh
```

## 흐름

이슈 → 브랜치 `<type>/<issue#>-<slug>` → PR → **스쿼시 머지** (브랜치 자동 삭제).
`main` 직접 푸시는 브랜치 보호가 막습니다. Conventional Commits, 그리고 **영어만** —
코드, 주석, 커밋, 문서 전부.

**CI 잡 이름을 바꾸지 마세요.** `main` 은 이름이 지정된 상태 체크 여섯 개를 요구하고
GitHub은 잡의 *표시 이름*으로 매칭하기 때문에, 이름을 바꾸면 "요구되고, 기대되고, 영원히
도착하지 않는" 체크가 생깁니다 — 브랜치 보호 설정을 같이 고치기 전까지 `main` 이 머지
불가가 됩니다. 워크플로 파일 어디에도 그 사실이 안 적혀 있습니다. 잡 추가는 괜찮습니다.
이름 변경은 두 군데를 고치는 일이고, 두 번째 자리는 소유자만 만질 수 있는 저장소
설정입니다.

## 문서도 코드와 같은 기준입니다

`README.md` 는 무엇이 만들어져 있는지를 정확히 한 곳 — *What works today* 표 — 에만
적습니다. 다른 모든 섹션은 설계를 설명합니다. 기능을 넣었으면 그 표의 행을 뒤집으세요.
다른 곳에 두 번째 주장을 추가하지 마세요. `scripts/guards.sh` 가 기계적인 절반을
검사합니다: 관리되는 문서가 이름 붙인 저장소 경로는 모두 실재해야 합니다. 의미적인 절반 —
문장이 아직 참인가 — 은 리뷰어의 몫입니다. 아직 일부러 안 만든 경로는 검사를 지우지 말고
그 줄에 `guard:planned` 를 붙이세요.

## 푸시 게이트 (반드시 만나게 됩니다)

`.githooks/pre-push` 는 게이트 두 개이고, 일부러 다르게 동작합니다.

**헌법 가드가 먼저 돌고, 닫힌 채로 실패합니다.** `scripts/guards.sh` 는 CLAUDE.md가
절대적이라고 명시한 규칙들을 검사합니다 — 자격증명·기록 커밋 금지, 통합 테스트 CI 제외,
영어 전용 산출물, 원문과 발맞춘 한국어 트윈, 해석되는 위키 인용. 탈출구는 없습니다.
저 중 어느 것도 허용 가능한 예외가 없기 때문입니다. 직접 `./scripts/guards.sh` 를 먼저
돌려보세요. 1초쯤 걸리고, 출력이 고칠 것을 알려줍니다.

> `&&` 체인에서 `./scripts/guards.sh | tail` 은 조심하세요 — 파이프는 `tail` 의 종료
> 코드를 가져가고, 실패한 가드가 정확히 그 길로 한 번 빠져나갔습니다 (#194).

**그다음 위키 게이트, 열린 채로 실패합니다.** 푸시 범위에 `docs/llm-wiki/` 변경이 없으면
막습니다. 이 저장소는 *결정을 기록하는 것*을 작업의 일부로 치기 때문입니다: 브랜치에서
무언가를 결정했다면 `docs/llm-wiki/wiki/decisions/` 아래에 ADR을 추가하세요 (형식은 기존
것들을 보세요). 정말 기록할 게 없는 변경(오타 수정)은 감사 가능한 트레일러를 남기세요:
`git commit --amend --no-edit --trailer 'Wiki-Skip: <이유>'`.

이 비대칭이 핵심입니다. 가드는 **위반**을 막고, 위키 게이트는 **무의식적 누락**을
막습니다 — 그리고 이유를 댈 수 있는 누락은 누락이 아닙니다.

## AI 보조 PR 환영합니다

이 프로젝트 자체가 AI 보조로 만들어졌습니다. 요건: PR에 그렇다고 밝히고(템플릿의
체크박스), 무엇을 직접 검증했고 어떻게 했는지 적고, 모든 줄을 설명할 수 있어야 합니다 —
"모델이 썼습니다"는 리뷰 답변이 아닙니다.

## 보안 / 프라이버시

세션 쿠키, 이메일, 개인 풀이 이력이 이 도구를 지나갑니다. 어느 것도 커밋하지 마세요.
테스트 픽스처는 반드시 스크럽하세요 (development-rules §7). 취약점은 공개 이슈 대신
소유자에게 이메일로 알려주세요 (프로필 참고).
